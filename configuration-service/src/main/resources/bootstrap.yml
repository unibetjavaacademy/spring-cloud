spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      enabled: false
      server:
        git:
           uri: https://github.com/unibetjavaacademy/spring-cloud/blob/master/configuration-service/src/main/resources/config
encrypt:
  failOnError: false
